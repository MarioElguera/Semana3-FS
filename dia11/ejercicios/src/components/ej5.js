import React from 'react';

const Card = () => {
    return (
        <div className="card">
            <h2 className="cardTitle">Tarjeta Interactiva</h2>
            <p className="cardContent">¡Pasa el mouse para ver el efecto!</p>
            <p className="cardDescription">Esta es una tarjeta que cambia de tamaño y color.</p>
        </div>
    );
};

export default Card;
